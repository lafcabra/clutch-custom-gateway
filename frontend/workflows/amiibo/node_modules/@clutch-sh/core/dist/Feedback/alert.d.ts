import * as React from "react";
import type { AlertProps as MuiAlertProps } from "@material-ui/lab";
export interface AlertProps extends Pick<MuiAlertProps, "severity" | "action" | "onClose" | "elevation" | "variant"> {
    title?: React.ReactNode;
}
export declare const Alert: React.FC<AlertProps>;
export interface AlertPanelProps {
    direction?: "row" | "column";
    children: React.ReactElement<AlertProps> | React.ReactElement<AlertProps>[];
}
export declare const AlertPanel: ({ direction, children }: AlertPanelProps) => JSX.Element;
