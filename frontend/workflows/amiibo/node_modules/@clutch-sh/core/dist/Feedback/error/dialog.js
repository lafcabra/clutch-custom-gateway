import React from "react";
import { Dialog, DialogContent } from "../../dialog";
import Code from "../../text";
const ErrorDetailsDialog = ({ error, open, onClose }) => {
    const prettyPrintError = JSON.stringify(error, undefined, 2);
    return (React.createElement(Dialog, { title: "Full Error Details", onClose: onClose, open: open },
        React.createElement(DialogContent, null,
            React.createElement(Code, null, prettyPrintError))));
};
export default ErrorDetailsDialog;
