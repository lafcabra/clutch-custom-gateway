import type { clutch as IClutch } from "@clutch-sh/api";
import type { ClutchError } from "@clutch-sh/core";
declare const fetchResourceSchemas: (type: string) => Promise<IClutch.resolver.v1.Schema[]>;
export interface ResolutionResults {
    results: object[];
    failures: {
        message: string;
    }[];
}
declare const resolveResource: (type: string, limit: number, fields: {
    [key: string]: any;
}, onResolve: (resultObjects: any[], failureMessages: string[]) => void, onError: (message: ClutchError) => void, apiPackage?: any) => Promise<void>;
export { fetchResourceSchemas, resolveResource };
