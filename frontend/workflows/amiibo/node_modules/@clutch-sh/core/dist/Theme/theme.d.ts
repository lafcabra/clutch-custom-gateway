import React from "react";
interface ThemeProps {
    variant?: "light" | "dark";
    children: React.ReactNode;
}
declare const ThemeProvider: ({ children, variant }: ThemeProps) => JSX.Element;
export default ThemeProvider;
