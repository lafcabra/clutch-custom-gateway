import * as React from "react";
import type { ClickAwayListenerProps, ListItemProps, PopperProps as MuiPopperProps } from "@material-ui/core";
export interface PopperItemProps extends Pick<ListItemProps, "selected"> {
    children: React.ReactNode;
    component?: React.ElementType;
    componentProps?: any;
    onClick?: () => void;
    icon?: React.ReactElement;
}
declare const PopperItem: ({ children, componentProps, onClick, icon, ...props }: PopperItemProps) => JSX.Element;
export interface PopperProps extends Pick<ClickAwayListenerProps, "onClickAway">, Pick<MuiPopperProps, "placement"> {
    open: boolean;
    anchorRef?: React.MutableRefObject<HTMLElement | null> | null;
    children?: React.ReactElement<PopperItemProps> | React.ReactElement<PopperItemProps>[];
    id?: string;
}
declare const Popper: ({ open, anchorRef, onClickAway, placement, children, id, }: PopperProps) => JSX.Element;
export { Popper, PopperItem };
