import React from "react";
declare enum WizardAction {
    NEXT = 0,
    BACK = 1,
    RESET = 2
}
interface StateProps {
    activeStep: number;
}
declare const useWizardState: () => [StateProps, React.Dispatch<WizardAction>];
export { WizardAction, useWizardState };
