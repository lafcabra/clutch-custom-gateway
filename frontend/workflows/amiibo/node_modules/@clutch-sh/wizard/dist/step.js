import React from "react";
import { Error, Loadable, styled, useWizardContext } from "@clutch-sh/core";
import { Grid as MuiGrid } from "@material-ui/core";
const Grid = styled(MuiGrid)({
    width: "100%",
    "> *": {
        margin: "8px 0",
    },
});
const WizardStep = ({ isLoading, error, children }) => {
    const wizardContext = useWizardContext();
    const hasError = error !== undefined && error !== null;
    const showLoading = !hasError && isLoading;
    React.useEffect(() => {
        wizardContext.setIsLoading(showLoading);
    }, [showLoading]);
    React.useEffect(() => {
        wizardContext.setHasError(hasError);
    }, [error]);
    if (showLoading) {
        return React.createElement(Loadable, { isLoading: isLoading }, children);
    }
    return (React.createElement(Grid, { container: true, justify: "center", direction: "column", alignItems: "stretch" }, hasError ? React.createElement(Error, { subject: error }) : children));
};
export default WizardStep;
